"use client"

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => { console.log("hello world") }}>click me</button>

        </div>
    )
}

export default AddToCart
